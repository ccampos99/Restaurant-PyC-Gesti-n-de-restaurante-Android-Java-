<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/blue_low_gradient"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:spacing="8dp">

        <!-- TÍTULO -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Dashboard de Caja"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginBottom="12dp" />

        <!-- TARJETAS DE RESUMEN -->
        <include layout="@layout/item_resumen" android:id="@+id/card_reservas" />
        <include layout="@layout/item_resumen" android:id="@+id/card_pedidos" />
        <include layout="@layout/item_resumen" android:id="@+id/card_platos" />
        <include layout="@layout/item_resumen" android:id="@+id/card_proveedores" />
        <include layout="@layout/item_resumen" android:id="@+id/card_ingredientes" />
        <include layout="@layout/item_resumen" android:id="@+id/card_empleados" />

    </LinearLayout>
</ScrollView>
